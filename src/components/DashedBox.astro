---
const { title } = Astro.props;
---

<div
  class="p-5 border-4 border-gray-600 dark:border-white border-dashed max-w-xl mx-auto"
>
  {
    title && (
      <h3 class="text-2xl font-bold text-center mb-3">{title}</h3>
    )
  }
  <slot />
</div>
