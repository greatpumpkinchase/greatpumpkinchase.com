---
const { href, bold = false } = Astro.props;
const path = Astro.url.pathname.replace(/\/$/, '') || '/';
const active = href === path;
---
<li><a class:list={['nav-link', { active, register: bold }]} href={href}><slot /></a></li>
