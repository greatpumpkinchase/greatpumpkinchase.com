# Repository Agent Instructions

- Never commit to or push the `main` branch unless the user gives specific, explicit instructions to do so in the current request.
- Before committing or pushing any branch, confirm that the user has explicitly requested that action.
